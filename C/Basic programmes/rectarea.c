#include<stdio.h>
int main()
{
    int l,b,a;
        printf("Lenght of the rectangle: ");
        scanf("%ld",&l);
        printf("\n Breadth of the rectangle: ");
        scanf("%ld",&b);
        a=l*b;
        printf("Area of the rectangle is %ld",a);
        return 0;
}
